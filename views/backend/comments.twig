{% extends 'views/templates/admin_template.twig' %}

{% block title %} GESTION DES COMMENTAIRES {% endblock %}

{% block content %}

{% for comments in comments %}
<div class="row>">
    <div class="col-lg-6 col-md-6 col-sm-12">
     <div class="comment">
        <div class="comment_info">
            <p>Commentaire publié par <b>: {{comments.pseudo}}</b><em> le {{comments.commentUpdate}}</em></p>
        </div>
           <p>Article : <b>{{comments.postTitle}}</b></p>
           <p>Contenu : {{comments.commentContent}}</p>
            <!-- "Publier" and "Refuser" buttons for each comment -->
            <a href="/admin/published/{{comments.commentId}}">
            <button  class="btn btn-warning btn-sm btn-sm-active" name="publier{{comments.commentId}}">Publier</button> </a>
            <a href="/admin/refused/{{comments.commentId}}">
            <button  class="btn btn-warning btn-sm btn-sm-active" name="refuser{{comments.commentId}}">Refuser</button></a><br>
        </div><br>
    </div>
</div>

{% endfor %}
            
{% endblock %}


{% block pagination %}

<p style="text-align : center;">Page :

{% for i in range(1, pagesNbr) %}

    {% if i == currentPage %}
            
        [ {{i}} ]

    {% endif%} 
            
    {% if i != currentPage %}

        <a href="/admin/comments/{{i}}">{{i}}</a>

    {% endif%}

{% endfor %} </p>

{% endblock%}


